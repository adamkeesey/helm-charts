apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-deployments.fullname" . }}-test-connection"
  labels:
    {{- include "go-deployments.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-deployments.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
